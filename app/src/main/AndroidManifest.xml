<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.adrreport">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Questions" />
        <activity android:name=".MainActivity" />
        <activity android:name=".SignUpAcivity" />
        <activity android:name=".BannedMedicineActivity" />
        <activity android:name=".SectionOneActivity" />
        <activity android:name=".SectionTwoActivity" />
        <activity android:name=".SectionThreeActivity" />
        <activity android:name=".SectionFourActivity"></activity>
    </application>

</manifest>